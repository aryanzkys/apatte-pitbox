# Shared Config

What belongs here:
- Shared linting, formatting, and build configuration presets
- Common environment variable conventions and templates
- Cross-repo tooling config that can be reused by apps

What must NOT belong here:
- App-specific runtime configuration
- Secrets or credentials
- Service-specific deployment files
