# Shared Types

What belongs here:
- TypeScript types for telemetry contracts and domain models
- Shared enums, interfaces, and schema adapters
- Versioned, backward-compatible type definitions

What must NOT belong here:
- Business logic or runtime implementations
- App-specific view models
- Generated code tied to a single service
