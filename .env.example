# Apatte Pitbox - Environment Template
# Copy to .env for local development.
# Do NOT commit real secrets.

# General
NODE_ENV=development
LOG_LEVEL=info

# Supabase (local dev)
# Required for web-dashboard once implemented
# Values are printed by `pnpm supabase:status` after `pnpm supabase:start`
SUPABASE_URL=http://localhost:54321
SUPABASE_ANON_KEY=REPLACE_WITH_VALUE_FROM_SUPABASE_STATUS
# Optional for now. DO NOT expose to client.
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_VALUE_FROM_SUPABASE_STATUS

# Next.js public Supabase vars (web-dashboard)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=REPLACE_WITH_VALUE_FROM_SUPABASE_STATUS

# MQTT (future use)
# Required for telemetry-ingestion and edge-agent once implemented
MQTT_BROKER_URL=mqtt://localhost:1883
MQTT_USERNAME=apatte
MQTT_PASSWORD=apatte-dev
MQTT_CLIENT_ID=

# Telemetry ingestion defaults
TELEMETRY_TOPIC_PREFIX=apatte
INGESTION_BATCH_SIZE=200
INGESTION_FLUSH_MS=250

# App ports (optional)
WEB_PORT=
INGESTION_PORT=
EDGE_PORT=
